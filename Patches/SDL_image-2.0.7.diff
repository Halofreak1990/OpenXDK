Only in xbox/SDL_ttf-2.0.7: Makefile.xbox
diff -r -u orig/SDL_ttf-2.0.7/SDL_ttf.c xbox/SDL_ttf-2.0.7/SDL_ttf.c
--- orig/SDL_ttf-2.0.7/SDL_ttf.c	2004-12-16 16:39:54.000000000 +1100
+++ xbox/SDL_ttf-2.0.7/SDL_ttf.c	2005-01-27 09:07:48.000000000 +1100
@@ -31,13 +31,13 @@
 #include <alloca.h>
 #endif
 
-#ifdef HAVE_ALLOCA
-#define ALLOCA(n) ((void*)alloca(n))
-#define FREEA(p)
-#else
+//#ifdef HAVE_ALLOCA
+//#define ALLOCA(n) ((void*)alloca(n))
+//#define FREEA(p)
+//#else
 #define ALLOCA(n) malloc(n)
 #define FREEA(p) free(p)
-#endif
+//#endif
 
 #include <ft2build.h>
 #include FT_FREETYPE_H
